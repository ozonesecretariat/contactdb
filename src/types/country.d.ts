export interface Country {
  code: string;
  name: string;
  officialName: string;
  region: string;
  subregion: string;
}

export interface Region {
  code: string;
  name: string;
}

export interface Subregion {
  code: string;
  name: string;
}
